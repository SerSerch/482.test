import React, { Fragment } from 'react';
import Projects from 'containers/ProjectsContainer';

const Home = () => {
        return (
            <Fragment>
                <Projects />
            </Fragment>
        );
};

export default Home;